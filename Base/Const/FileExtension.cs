namespace Base
{
    //string ext = fileInfo.Extension.Remove(0, 1).ToLower();
    public enum FileExtension
    {
        xml,
        ds,
        dt,
        jproj,
        mdf,
        db,
        mdb
    }
}
